package UI;

import javax.swing.*;

public class QuizzesMenu extends JFrame {
    public QuizzesMenu () {
        setTitle("Quizzes Menu");
        setSize(1920, 1080);
        setLocationRelativeTo(null);
        setDefaultCloseOperation(EXIT_ON_CLOSE);
    }
}
